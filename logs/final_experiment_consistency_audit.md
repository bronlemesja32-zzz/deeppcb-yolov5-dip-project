# 最终实验数字一致性检查报告

- 检查日期：2026-07-12
- 检查对象：`pcb_project/report/main.tex`、`pcb_project/report/main.pdf` 及其引用的表格文件
- 检查范围：现有数据转换报告与 manifest、YOLOv5s 训练/评估日志与 CSV、传统图像处理基线 CSV/报告
- 检查方式：只读检索、CSV 行数/字段求和与数值对照、PDF 文本只读提取；未运行任何实验脚本

## 1. 总结结论

**结论：通过。**

报告中的指定核心实验数字均能在当前项目保留的日志、CSV、YOLO 输出或数据转换报告中找到一致来源；
`main.tex`、最终 `main.pdf` 与这些来源之间未发现核心数字不一致。YOLO 和传统基线在报告中使用的
三位小数均与结构化结果的原值或正确舍入一致。

最终 PDF 为 8 页，文件时间晚于当前 `main.tex`；只读提取确认 PDF 中包含与源码一致的总体指标、
分类别指标、数据划分、传统基线指标和必要的协议限定。

## 2. 已核对且一致的项目

### 2.1 数据链一致性

| 核对项 | 报告值 | 只读证据 | 结论 |
|---|---:|---|---|
| 官方样本/待测图数量 | 1500 | `datasets/deeppcb/manifest.csv` 共 1500 行；官方 `trainval.txt` 1000 行、`test.txt` 500 行；原始目录有 1500 张 `*_test.jpg`，每张均有对应模板 | 一致 |
| train/val/test 图像数 | 900 / 100 / 500 | manifest 分组统计；`logs/conversion_report.md`；`logs/verify_report.md` | 一致 |
| train/val/test 框数 | 6199 / 674 / 3140 | manifest 的 `num_boxes` 分 split 求和；转换报告与自检报告 | 一致 |
| 总框数 | 10013 | manifest 求和；`verify_report.md` 分类别合计；报告表 1/表 2 | 一致 |
| 类别映射 | open=0, short=1, mousebite=2, spur=3, copper=4, pin_hole=5 | `02_convert_to_yolo.py` 的 `NAMES` 与 `class_id=type-1`；`deeppcb.yaml`；`conversion_report.md` | 一致 |
| 官方 split 对应关系 | test.txt→test；trainval.txt→train+val | 官方列表与 manifest 的 image_id 集合逐项相等，缺失/额外均为 0；train/val 交集为 0 | 一致 |
| val 抽样 | trainval 1000 张中按 group 最大余数法抽 100 张，seed=20260712 | `02_convert_to_yolo.py`、`conversion_report.md`、`deeppcb.yaml` 注释 | 一致 |

补充核对：manifest 中 train+val 的 1000 个 image_id 与官方 `trainval.txt` 完全相同，test 的 500 个
image_id 与官方 `test.txt` 完全相同。报告关于“官方 test 在 YOLO 权重固定后才用于最终评估”的表述
与转换说明和训练/评估日志一致。

### 2.2 YOLOv5s 训练与评估一致性

#### 官方测试集总体与分类别指标

原始复跑日志 `logs/test_eval_repro_stderr.log` 的指标表、结构化文件
`logs/test_eval_repro_metrics.csv`、`logs/test_eval_report.md` 与报告表 3/表 4 逐项一致：

| 类别 | P | R | mAP50 | mAP50:95 | 结论 |
|---|---:|---:|---:|---:|---|
| all | 0.951 | 0.944 | 0.968 | 0.594 | 一致 |
| open | 0.973 | 0.973 | 0.976 | 0.547 | 一致 |
| short | 0.936 | 0.863 | 0.912 | 0.482 | 一致 |
| mousebite | 0.969 | 0.951 | 0.981 | 0.572 | 一致 |
| spur | 0.985 | 0.927 | 0.970 | 0.606 | 一致 |
| copper | 0.980 | 0.970 | 0.982 | 0.734 | 一致 |
| pin_hole | 0.865 | 0.979 | 0.986 | 0.623 | 一致 |

复跑日志明确回显 `task=test`、权重为正式训练的 `best.pt`、Images=500、Instances=3140；原评估目录
和复跑目录均保留 500 个预测 txt。复跑指标在日志显示精度下与首次评估报告完全相同。

#### val 指标与最优权重

- `logs/formal_train_console.err.log` 的 best.pt 最终验证行记录：100 图、674 框，
  **P=0.978、R=0.973、mAP50=0.985、mAP50:95=0.651**；与报告一致。
- 正式 run 的 `yolov5/runs/train/deeppcb_yolov5s_50ep/results.csv` 共 50 行（epoch 0--49）。
  按 YOLOv5 的 fitness `0.1*mAP50 + 0.9*mAP50:95` 对现有 CSV 行做只读排序，最大值位于
  **epoch 38**：P=0.97748、R=0.97294、mAP50=0.98511、mAP50:95=0.64837，
  fitness=0.682044。报告“best.pt 对应第 38 epoch”一致。
- `weights/best.pt` 当前存在；训练日志记录随后对该文件执行最终 val 验证。

#### 两个 seed 与 test 用途

- 正式 run 的 `opt.yaml` 和训练日志参数行均记录 `seed: 0`，对应 train.py 的训练随机种子。
- 转换脚本、转换报告与数据 YAML 均记录数据划分 seed=20260712。
- 报告明确将两者区分；未把 20260712 写成训练 seed。
- 训练日志只扫描 train/val，best.pt 的选择指标在 val 上计算；正式训练报告明确训练阶段尚未进入 test。
  报告也明确 test 仅在模型训练完成、权重固定后用于最终评估。因此不存在暗示 test 参与 YOLO 训练
  或最优权重选择的表述。

### 2.3 传统图像处理基线一致性

`outputs/ip_baseline/ip_baseline_metrics.csv` 与报告表 5 对照如下：

| IoU | TP | FP | FN | CSV P/R/F1 | 报告 P/R/F1 | 其他统计 | 结论 |
|---:|---:|---:|---:|---|---|---|---|
| 0.33 | 2124 | 1194 | 1016 | 0.6401 / 0.6764 / 0.6578 | 0.640 / 0.676 / 0.658 | 候选 3318；6.636/图；4 张零候选 | 一致（正确舍入） |
| 0.50 | 2092 | 1226 | 1048 | 0.6305 / 0.6662 / 0.6479 | 0.631 / 0.666 / 0.648 | 候选 3318；6.636/图；4 张零候选 | 一致（正确舍入） |

对 `ip_baseline_per_image.csv` 的 500 行只读求和再次得到：候选框 3318、真值框 3140、
TP/FP/FN 分别为上述两组数字；零候选图恰为 4 张
（20085298、20085320、44000017、44000053）。

#### PAD 标定

- `05_ip_baseline.py` 固定 `PAD_PX=10`，注释和代码均说明该值在 val 标定后冻结并应用于 test。
- `06_export_pad_calibration.py` 只筛选 manifest 的 `split=val`；CSV 记录 100 图、674 框、684 个
  原始候选，不读取 test 行。
- `pad_calibration.csv` 与报告表 7 的 F1 对照全部一致：

| PAD | CSV F1@0.33 / F1@0.50 | 报告（三位小数） | 结论 |
|---:|---|---|---|
| 0 | 0.0619 / 0.0059 | 0.062 / 0.006 | 一致 |
| 4 | 0.5567 / 0.1340 | 0.557 / 0.134 | 一致 |
| 6 | 0.6907 / 0.4978 | 0.691 / 0.498 | 一致 |
| 8 | 0.6996 / 0.6657 | 0.700 / 0.666 | 一致 |
| 10 | 0.7025 / 0.6878 | 0.703 / 0.688 | 一致；双阈值最优 |
| 12 | 0.6996 / 0.6863 | 0.700 / 0.686 | 一致 |
| 14 | 0.6996 / 0.6819 | 0.700 / 0.682 | 一致 |
| 16 | 0.6966 / 0.6318 | 0.697 / 0.632 | 一致 |

#### 方法定位与指标口径

- `main.tex` 明确写明传统方法是类别无关候选方法、不输出六类类别、不是六类分类器；
  与 `05_ip_baseline.py` 和 `ip_baseline_report.md` 一致。
- 报告明确说明 YOLO mAP 与传统 F1 来自不同协议，不能直接作数值比较；未把两者作为同一指标排名。
- `ip_baseline_per_image.csv` 中 `yolo_fn_033` 合计为 79，`ip_only_gt_033` 合计为 49，
  后者分布在 43 张图。这里的 YOLO 匹配是脚本注明的 `conf>=0.25`、IoU=0.33、类别无关案例筛选口径。
  报告原文保留了 `conf>=0.25` 限定，只说“43 张图上的 49 个漏检真值框被传统方法找回”，
  没有写成正式 YOLO 指标下的全部 FN 被找回。

## 3. 发现的不一致或无法确认项目

### 核心实验数字

**未发现不一致；上述指定核心数字均可确认。**

### 文件层补充说明（不影响实验统计）

原始 `DeepPCB/PCBData` 目录只读枚举得到 1500 张 `*_test.jpg` 和 1501 张 `*_temp.jpg`；多出的
`group90100/90100/90100034_temp.jpg` 没有对应待测图，也未被官方 `trainval.txt`、`test.txt` 或
manifest 引用。报告中的“1500”采用官方样本/待测图/有效图像对口径，转换、训练、评估和所有统计
均只涉及这 1500 个官方记录，因此这一个未引用模板文件不构成报告数字不一致。

原首次 test 评估没有单独命名的完整控制台日志文件，但当前项目保留了同权重、同数据、同参数的
复跑原始 stderr 和结构化 CSV，且与首次 `test_eval_report.md` 逐项相同；因此测试指标仍可确认，
不列为缺失证据。

## 4. 需要人工确认但不建议新增实验的项目

无需新增实验，也没有阻止提交的人工确认项。

若课程教师把“总图像数”理解为磁盘中所有 JPG 文件数，可人工说明：实验口径是官方 split 中的
1500 个待测样本（每个均有模板），不是把未被官方列表引用的孤立模板文件计为额外样本。该说明不需要
重新转换、训练或评估。

## 5. 实验真实性相关高风险表述检查

| 风险项 | 检查结果 |
|---|---|
| “SOTA”或同义领先声明 | `main.tex` 与最终 PDF 中均未出现 |
| 把 mAP50=0.968 写成 accuracy/“准确率 96.8%” | 未出现；始终写作 mAP50 0.968 |
| 正面声称工业部署能力 | 未出现；相关语句均是否定限定：“不应引申为工业场景泛化能力”“不能直接外推到真实工业部署” |
| 把传统方法写成六类分类器 | 未出现；报告明确写“不是六类分类器”及“不输出六类类别” |
| 把 49 个找回写成正式 YOLO FN 全部找回 | 未出现；报告明确保留 `conf>=0.25`，且只写 49 个/43 张图 |
| 报告数字与日志/CSV 不一致 | 未发现 |

**最小修改建议：无。** 当前报告不含需要为实验真实性而修改的危险表述。

## 6. 本次检查的操作边界

本次检查：

- 没有训练；
- 没有评估或复跑模型；
- 没有运行数据转换、传统基线或 PAD 标定脚本；
- 没有安装、升级或卸载任何包；
- 没有修改 `main.tex`、PDF、CSV、图片、日志、模型权重或其他实验产物；
- 没有要求新增实验、多 seed、hash/近重复检测或工业部署验证；
- 仅新增本检查报告 `pcb_project/logs/final_experiment_consistency_audit.md`。
